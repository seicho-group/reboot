import {ButtonHTMLAttributes, DetailedHTMLProps, PropsWithChildren} from "react";import styles from "./button.module.scss"type TProps = DetailedHTMLProps<ButtonHTMLAttributes<HTMLButtonElement>, HTMLButtonElement>export const Button = ({children, ...props}: PropsWithChildren<TProps>) => {    return <button {...props} className={styles.button}>{children}</button>}