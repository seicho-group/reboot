import styles from "./hero.module.scss"import {HeroImage} from "@/components/hero/hero-image/hero-image";import {Button} from "@/ui/button/button";export const HeroSection = () => {    return (        <div className={styles.hero}>            <HeroImage/>            <div className={[styles.heroDescription, "container"].join(" ")}>                <p className={styles.heroDescriptionText}>Смартфонов, компьютеров и бытовой техники в <span className={"blue"}>г. Перми</span></p>                <h1 className={styles.heroDrunkText}>REМОНТ</h1>                <Button>Записаться</Button>            </div>        </div>    )}